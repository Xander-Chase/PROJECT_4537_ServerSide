export const CORS_YOUR_ORIGIN = "*" // Change this to your localhost origin
// https://termprojecto.teddyd-website.xyz

export const MappedEndpoints = {
    Auth: "/api/auth",
    Admin: "/api/admin",
    User: "/api/user", // Generate Story, Update Story, Everythin else
}
