export const CORS_YOUR_ORIGIN = "http://127.0.0.1:3000" // Change this to your localhost origin

export const MappedEndpoints = {
    Auth: "/api/auth",
    Admin: "/api/admin",
    User: "/api/user", // Generate Story, Update Story, Everythin else
}
